# 街舞猫（双端）

- ID：`laowu:street_dance`；优良、不可升级，占用外观互斥位。
- 速度属性+10、耐力属性+10，沿用统一有效属性计算，不改写基因。
- 动作来自已确认的 `docs/blockbench/cat_thomas_flare.bbmodel`，不再使用旧的躯干自转程序。
- Blockbench 源动画 2.4 秒一圈；游戏播放倍率 2.0，实际 1.2 秒 / 24 tick 一圈。
- 采用单根腿骨骼，八骨骼共享 145 帧关键姿势；保留独立扫腿、交替撑地、平滑翻转和躯干下沉。
- 纯客户端骨骼动画，不旋转实体朝向、不修改碰撞箱、不添加逐帧网络包。
- 坐下、骑乘、睡觉、哈气、猫饼姿态、战斗、离地和水中暂停；地面移动允许播放。
- 每次准备原版姿态前复位骨骼，避免共享模型把街舞姿态泄漏到其他猫。
- 职业套装的骨骼变换包含缩放，身体附件先回到身体局部坐标再缩放、旋转；普通和融合材质使用原渲染路径。
- 项圈使用原版略微外扩的模型，但直接复制猫咪本帧的骨骼位移、旋转、缩放和幼年状态，不再独立计算原版动作；街舞时随头部移动，哈气和猫饼沿用各自的项圈路径。

## 资源与维护

`tools/export-thomas-flare.mjs` 从已保存的 Blockbench 文件导出 ModelPart 坐标的精简关键帧 JSON，标准输出供应用补丁写入使用。两端文件均位于 `assets/laowu/animations/cat_thomas_flare.json`。

导出器保留源文件哈希以及 Blockbench 实际保存的时间戳。`node tools/export-thomas-flare.mjs --check` 检查两端资源与源模型一致。源动画维持原速，二倍速只由 Java 播放器控制，避免重复加速。

客户端仅在类首次使用时加载模组自带的动画资源一次，所有猫共享不可变数据，播放时只做插值。资源缺失或损坏时记录错误并保留原版姿态，不阻止进入世界；更换动画包后须重启游戏。

## 验证（2026-09-10）

- Forge / NeoForge 均构建成功。
- `tests/run-thomas-flare.ps1`：各检查 961 个姿势、61504 个顶点，与原始 Blockbench 关键帧逐项对照；最大坐标误差约 0.000144 像素。
- 双倍速周期、首尾衔接、前爪持续撑地、身体/后腿不穿地及套装缩放变换测试通过；撑地误差约 0.005936 像素。
- 项圈修复回归：`tests/run-cat-collar.ps1` 对两端各检查 1922 个成猫/幼猫姿势，验证八骨骼完整跟随、缩放/可见性复位以及外扩网格不被替换。使用真实 Minecraft 模型类（NeoForge 的无窗口测试使用 NeoForm 命名原版模型，不启动 FML）。
- `tests/run-thomas-flare.ps1 -MissingResource`：两端缺失资源时安全回退测试通过。
- 构建产物部署到 PCL 的 Forge 1.20.1 和 NeoForge 1.21.1 模组开发实例，旧包保留为禁用备份。
- 已完成数学/资源/构建验证；实际游戏中的视觉效果仍需启动实例检查。
