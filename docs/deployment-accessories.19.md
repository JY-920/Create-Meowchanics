# accessories.19：医疗群疗、坐垫诊疗与套装平衡

2026-09-14 20:19 已部署。公开版本仍为 2.1.4，内部构建 2.1.4-dev.accessories.19。

## 本次行为

- 医疗以有效智力乘存档/全局倍率后的值计算：每秒回复 1 + 0.03 × 智力，100 智力为每秒 4 点；半径 min(32, 3 + 0.03 × 智力)，100 智力为 6 格。1 秒准备，每 0.25 秒分次治疗。
- 战斗时靠近友方受伤职业猫后，原地群体治疗，包含自己；患者保持自己的动作、寻路、战斗或飞行挂载。空中患者按身体中心三维距离判定，医疗猫向其下方靠近，不再强求进入旧 2.5 格近身距离。
- 同 tick 多来源先汇总，服务端 tick 末仅采用最强医疗速率；患者共用持久化治疗间隔，战斗圈和坐垫诊疗不能叠加。
- 医疗猫在 Create 坐垫上工作时，身体中心固定 3×3×3 范围内的伤损生物均可受疗，包括自己、玩家、普通猫、被动和敌对生物。要求患者中心在范围内且无遮挡；不含盔甲架或旁观者。范围固定，速率随智力，移除坐垫即停止。
- 施法姿态改为明显抬身张爪和稳定呼吸，只限制医疗猫移动；受疗实体添加绿色姿态轮廓。十字图标放大，五个上升十字外增加持续头顶十字；地形正常遮挡，没有治疗弹幕。坐垫模式保留乘坐/坐下指令但允许治疗动作。
- 工程套装保留 +30 生命、+12 护甲、+5 韧性，六维仅 +10 幸运；原战斗力 +10、智力 +20、幸运 +40 调整为 0/0/10，其他加成和炮台机制不变。
- 医疗套装为 +6 生命、+2 护甲、+0 韧性、+10 速度，保留辅助职业同属性下移动快 20%。中英文说明和 Shift 示例同步。
- 旧配置迁移版本升至 3：只升级完整匹配原默认值的工程六维组、医疗生存组；任一自定义值保留整组。既有飞行等平衡不会对已迁移存档重复执行，钓鱼与全局覆盖不改动。

通信版本 Forge 25 → 26、NeoForge 18 → 19。医疗包扩展到 LivingEntity，新增半径/坐垫模式，玩家自身也接收跟踪包；客户端与服务端必须一起更新。

## 验证

- 双端 build 各 23,120 项饰品检查通过；正式包包含 CatMedicalWork、MedicalPatientLayer 和新的医疗通信包，不包含 GameTest/客户端探针。
- 双端无 KubeJS GameTest 各 20 项全通过；覆盖真实 Create 坐垫、真实扳手/载人飞行、医疗自疗、100 智力准确 4 HP/s、患者/施法者状态隔离、固定立方体边界、玩家/普通生物治疗与停止。
- 双端有 KubeJS GameTest 各 20 项最终全通过，包含真实脚本事件、取消与重载验证。Forge 的旧自动炮台测试首轮有一次未在时限内完成架炮，补充测试场地残留实体清理及诊断后复测通过；未据此改动炮台生产 AI，也未断言该次超时原因已经证实。
- 新增最高速率合并测试验证强弱来源执行顺序无关，同 tick 重复提交不能越过患者间隔；真实 ByteBuf/StreamCodec 验证通用实体 UUID、半径和工作模式。
- 配置回归含各 139,239 项套装断言，遍历旧 revision 0/1/2、自定义组掩码、全局覆盖与钓鱼保留。
- 医疗静态接线 65、飞行 133、饰品 391、职业伤害 350 项通过。
- 双端真实 GPU 客户端探针通过：安装于 Forge 81 / NeoForge 84 个 LivingEntity 渲染器，含两种玩家皮肤；实际猫模型的绿色轮廓与十字、抬身张爪骨骼参数和原紫色演奏轮廓通过。
- 世界空间十字在非零相机坐标、3 种视角下使用实际投影路径正常输出，近处地形深度遮挡测试通过；已查看输出图片。
- 旧测试中的工程/医疗属性断言按本次批准值更新；飞行测试夹具使用同一个真实主人，坐垫夹具先执行 positionRider 再测范围。测试场景使用独立 build 目录，不改玩家存档。

这些自动化检查不替代用户在完整整合包中实际登录、多视角观察医疗动作和仓管 UI 验收。保留既有 GL 状态隔离与恢复，未因本次医疗修改宣称仓管 UI 问题已修复。

## 部署

部署前确认没有运行中的 Minecraft 客户端。两台本机测试服于 19:54 通过正常 stop 保存并关闭，日志确认所有维度已保存，RCON 监听已停止；按用户要求，换包后未重新启动。

四处均仅启用一个 modId=laowu 的 accessories.19 包，新包与构建产物 SHA-256 相同：

- Forge 客户端与本机测试服：6ABAD33F075876A38B18A2F090BF887E5848B4AE78F9A9D20B257146A772D7E6
- NeoForge 客户端与本机测试服：BE91898B9EA1B2BD5900FB88D2636ACF81E2DD28C42C4B884CF9A8368FCAC60D

安装目录：

- D:/PCL2/PCL/.minecraft/versions/1.20.1-Forge模组开发/mods
- D:/PCL2/PCL/.minecraft/versions/1.21.1-NeoForge模组开发/mods
- D:/PCL2/PCL/.minecraft/versions/1.20.1-Forge模组开发/local-multiplayer-server/mods
- D:/PCL2/PCL/.minecraft/versions/1.21.1-NeoForge模组开发/local-multiplayer-server/mods

四处旧 accessories.18 均移入各自实例根目录下的 mod-backups/create-meowchanics/20260914-201955-accessories.19/，备份 SHA 与换包前一致，可恢复。部署脚本先校验并暂存所有新包，再可回滚地替换；未改用户存档、配置或其他模组。未提交、推送或发布公开版本。
