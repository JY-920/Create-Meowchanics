# 2026-09-09 激光笔染色与视角修正

- 改为工作台任意位置的 1 支激光笔 + 1 个染料，覆盖为相应 16 色，不混色，保留其他物品数据。JEI 提供 16 个固定配方。
- 删除副手染料 + 蹲下右键染色。蹲下右键始终切换归属预览，不消耗副手染料。
- 保存 LaserDye ID 并保留 LaserColor RGB；旧 RGB 数据仍可使用，原有标准染料色会匹配对应色名，默认红色。
- 描述常显“激光颜色：颜色名”。领养箱、航空箱、激光笔和收纳箱的简介按用户给定短文替换；收纳计数继续动态显示。
- 不再从 AFTER_SOLID_BLOCKS 缓存世界渲染矩阵。第一人称在 ItemInHandRenderer.renderHandsWithItems 入口记录本次手部渲染矩阵；第三人称在 LivingEntityRenderer.render 入口记录实体原点变换。使用本次渲染的 partial tick 和玩家视线，将目标换算到实际渲染坐标系，再对齐笔身与笔口光束。
- 指令发送、32 格范围及服务端目标校验不变。游戏内第一/第三人称、左右手、跳跃、转向及光影兼容仍需实测。
